diff --git a/codex-rs/windows-sandbox-rs/src/acl.rs b/codex-rs/windows-sandbox-rs/src/acl.rs
--- a/codex-rs/windows-sandbox-rs/src/acl.rs
+++ b/codex-rs/windows-sandbox-rs/src/acl.rs
@@ -236,6 +236,14 @@ pub unsafe fn dacl_has_write_deny_for_sid(p_dacl: *mut ACL, psid: *mut c_void)
         | GENERIC_WRITE_MASK
         | DELETE
         | FILE_DELETE_CHILD;
+    let mapping = GENERIC_MAPPING {
+        GenericRead: FILE_GENERIC_READ,
+        GenericWrite: FILE_GENERIC_WRITE,
+        GenericExecute: FILE_GENERIC_EXECUTE,
+        GenericAll: FILE_ALL_ACCESS,
+    };
+    let mut required_mask = deny_write_mask;
+    MapGenericMask(&mut required_mask, &mapping);
     for i in 0..info.AceCount {
         let mut p_ace: *mut c_void = std::ptr::null_mut();
         if GetAce(p_dacl as *const ACL, i, &mut p_ace) == 0 {
@@ -252,7 +260,12 @@ pub unsafe fn dacl_has_write_deny_for_sid(p_dacl: *mut ACL, psid: *mut c_void)
         let base = p_ace as usize;
         let sid_ptr =
             (base + std::mem::size_of::<ACE_HEADER>() + std::mem::size_of::<u32>()) as *mut c_void;
-        if EqualSid(sid_ptr, psid) != 0 && (ace.Mask & deny_write_mask) != 0 {
+        let mut mask = ace.Mask;
+        MapGenericMask(&mut mask, &mapping);
+        // A partial deny, such as the inherited FILE_DELETE_CHILD guard used
+        // on writable roots, is not a substitute for the full deny-write ACE
+        // required on protected children such as `.git`.
+        if EqualSid(sid_ptr, psid) != 0 && (mask & required_mask) == required_mask {
             return true;
         }
     }
